// Set this to the deployed Google Apps Script Web App URL before publishing.
// It ends in /exec, not /dev.
window.CLASS_QUESTIONS_CONFIG = {
  apiUrl: 'PASTE_YOUR_GOOGLE_APPS_SCRIPT_EXEC_URL_HERE',
  // Optional: a GitHub Pages folder containing lecture material.  Leave blank
  // when questions are the only content on the QR-code page.
  resourcesBaseUrl: ''
};
